<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="/css/bootstrap.min.css">
        <title></title>
    </head>
    <body>
        <h1 style="text-transform:uppercase"><%= heading %></h1>
        <ul>
            <% for(var i=0;i<Branch.length;i++){ %>
                <li><a href="<%= link %>/<%= Branch[i].Sid %>" > <%= Branch[i].Email %> </a> -<%= Branch[i].Sid %> </li>
            <% } %>
        </ul>
    </body>
</html>
