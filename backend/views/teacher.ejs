<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="/css/bootstrap.min.css">
        <title></title>
    </head>
    <body>
        <h1>DEMO</h1>
        <ul>
            <% for(var i=0;i<Teacher_details.length;i++){ %>
                <li><a href="/Teacher/<%= Teacher_details[i].Email %>"><%= Teacher_details[i].Email %></a> -<%= Teacher_details[i].username %> </li>
            <% } %>
        </ul>
    </body>
</html>
